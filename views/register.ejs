<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script>
        window.onload = function () {
            const urlParams = new URLSearchParams(window.location.search);
            const error = urlParams.get('error');

            if (error) {
                alert(error); // Hiển thị thông báo lỗi
            }
        };
    </script>
    <title>Login</title>
    <!-- <link rel="stylesheet" href="/styles.css"> Đường dẫn tới file CSS, nếu có -->
</head>

<body>
    <div class="login-container">
        <h2>Login</h2>
        <form action="/auth/register" method="POST">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="account_name">Name:</label>
                <input type="account_name" id="account_name" name="account_name" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Register</button>
        </form>
        <p> Come Back <a href="/auth/login">Login Here</a></p>
    </div>
</body>

</html>